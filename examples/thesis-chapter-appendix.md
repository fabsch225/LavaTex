# Appendix

The master note's `appendix-from: "[[thesis-chapter-appendix]]"` inserts a
raw `\appendix` right before this chapter, so it (and anything listed after
it) gets lettered (Appendix A, B, ...) instead of numbered.

```tikz
\begin{tikzpicture}
\draw[-Stealth] (0,0) -- (2,1);
\end{tikzpicture}
```
Figure: A TikZ diagram, exported as a numbered figure. {#fig:comm}

See figure [#fig:comm].

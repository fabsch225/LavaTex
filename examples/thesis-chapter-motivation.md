# Motivation

This is a chapter note, a plain note like any other — the master note's
`chapters:` list is what turns it into `\chapter{Motivation}` instead of a
standalone document. See [#thm-main] for the running example.

**Theorem** (Main result) {#thm-main}
Let $x \in \mathbb{R}$. Then $x^2 \geq 0$.
∎
